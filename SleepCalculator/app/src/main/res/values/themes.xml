<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SleepCalculator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SleepCalculator" parent="Base.Theme.SleepCalculator" />

    <!-- Custom TimePicker style -->
    <style name="CustomTimePicker" parent="ThemeOverlay.MaterialComponents.TimePicker">
        <item name="android:background">@color/timePickerBackground</item>
        <item name="android:textColor">@color/timePickerText</item>
        <item name="android:textColorPrimary">@color/timePickerText</item>
        <item name="android:textColorSecondary">@color/timePickerText</item>
        <item name="android:numbersBackgroundColor">@color/timePickerBackground</item>
    </style>
</resources>